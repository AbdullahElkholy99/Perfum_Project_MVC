@model VerifyCodeVM
@{
    Layout = null;
    ViewData["Title"] = "Verify Email";
}
<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verify Email — Sillage</title>
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400&family=Jost:wght@200;300;400;500&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
@*     <link href="~/css/users/customerpage.css" rel="stylesheet" />
    <link href="~/css/auth/auth.css" rel="stylesheet" /> *@

    <style>
body{
    margin:0;
    font-family:'Segoe UI', Tahoma, sans-serif;
    background: linear-gradient(135deg, #f6f9fc, #e9f1ec);
}

/* SECTION */
.verify-email-hero{
    min-height:100vh;
    display:flex;
    align-items:center;
    justify-content:center;
    padding:20px;
}

/* CARD */
.verify-box{
    width:100%;
    max-width:420px;
    background:#fff;
    padding:40px 30px;
    border-radius:16px;
    box-shadow:0 20px 50px rgba(0,0,0,0.1);
    animation:fadeUp .5s ease;
}

/* TITLE */
.verify-title{
    font-size:26px;
    text-align:center;
    margin-bottom:8px;
    font-weight:700;
}

/* SUBTEXT */
.verify-sub{
    text-align:center;
    margin-bottom:25px;
    color:#777;
    font-size:14px;
}

/* INPUT */
.verify-code-input{
    width:100%;
    padding:14px;
    border:1px solid #ddd;
    border-radius:10px;
    letter-spacing:10px;
    text-align:center;
    font-size:20px;
    outline:none;
    transition:0.3s;
margin:10px 0;
}

.verify-code-input:focus{
    border-color:#7a9e7e;
    box-shadow:0 0 0 3px rgba(122,158,126,0.2);
}

/* BUTTON */
.verify-btn{
    width:100%;
    padding:12px;
    border:none;
    background:#7a9e7e;
    color:#fff;
    font-size:15px;
    border-radius:10px;
    cursor:pointer;
    transition:0.3s;
    display:flex;
    justify-content:center;
}

.verify-btn:hover{
    background:#5f8463;
}

/* BACK LINK */
.verify-back{
    display:block;
    text-align:center;
    margin-top:10px;
    color:#7a9e7e;
    text-decoration:none;
    font-weight:600;
}

/* ERROR */
.auth-error{
    background:#ffe5e5;
    color:#d33;
    padding:10px;
    border-radius:8px;
    margin-bottom:15px;
    text-align:center;
    font-size:14px;
}

/* FIX ANIMATION */
@@keyframes fadeUp{
    from{
        opacity:0;
        transform:translateY(20px);
    }
    to{
        opacity:1;
        transform:translateY(0);
    }
}
    </style>
</head>
<body>


  @*   <section class="hero" style="min-height:60vh;display:flex;align-items:center;justify-content:center;">
        <div style="width:100%;max-width:420px;padding:0 24px;">
            <h1 class="hero-title" style="font-size:28px;text-align:center;margin-bottom:8px;">Verify <em>Email</em></h1>
            <p class="hero-sub" style="text-align:center;margin-bottom:16px;">We sent a 6-digit code to <strong>@Model.Email</strong> to confirm your account. Check your inbox (and spam folder).</p>

            @if (TempData["VerifyEmailMessage"] != null)
            {
                <div class="auth-msg auth-error">@TempData["VerifyEmailMessage"]</div>
            }
            <div asp-validation-summary="ModelOnly" class="auth-msg auth-error"></div>

            <form asp-action="VerifyEmailConfirmation" asp-controller="Account" method="post" class="auth-form">
                @Html.AntiForgeryToken()

                <input type="hidden" asp-for="Email" />

                <div class="auth-input-wrap">
                    <label asp-for="Code" class="auth-label">Verification Code</label>
                    <input asp-for="Code" type="text" class="auth-input" placeholder="000000" maxlength="6" autocomplete="one-time-code" style="letter-spacing:8px;text-align:center;font-size:20px;" />
                    <span asp-validation-for="Code" class="auth-validation"></span>
                </div>

                <button type="submit" class="hero-cta" style="width:100%;justify-content:center;margin-bottom:16px;">Verify</button>
                <a asp-action="Login" asp-controller="Account" class="auth-link" style="display:block;text-align:center;">Back to Sign In</a>
            </form>
        </div>
    </section>
 *@

    <section class="hero verify-email-hero">

        <div class="verify-box">

            <h1 class="hero-title verify-title">
                Verify <em>Email</em>
            </h1>

            <p class="hero-sub verify-sub">
                We sent a 6-digit code to
                <strong>@Model.Email</strong>.
                Check your inbox (and spam folder).
            </p>

            @if (TempData["VerifyEmailMessage"] != null)
            {
                <div class="auth-msg auth-error">
                    @TempData["VerifyEmailMessage"]
                </div>
            }

            <div asp-validation-summary="ModelOnly" class="auth-msg auth-error"></div>

            <form asp-action="VerifyEmailConfirmation"
                  asp-controller="Account"
                  method="post"
                  class="auth-form verify-form">

                @Html.AntiForgeryToken()

                <input type="hidden" asp-for="Email" />

                <div class="auth-input-wrap">

                    <label asp-for="Code" class="auth-label">
                        Verification Code
                    </label>

                    <input asp-for="Code"
                           id="verifyCode"
                           type="text"
                           class="auth-input verify-code-input"
                           placeholder="000000"
                           maxlength="6"
                           autocomplete="one-time-code" />

                    <span asp-validation-for="Code" class="auth-validation"></span>

                </div>

                <button type="submit" class="hero-cta verify-btn">
                    Verify Email
                </button>

                <a asp-action="Login"
                       asp-controller="Account"
                       class="auth-link verify-back">
                    Back to Sign In
                </a>

            </form>

        </div>

    </section>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/dist/jquery.validate.unobtrusive.min.js"></script>


<script>

const codeInput = document.getElementById("verifyCode");

codeInput.addEventListener("input", function () {

    this.value = this.value.replace(/[^0-9]/g, '');

});

codeInput.addEventListener("paste", function (e) {

    let paste = (e.clipboardData || window.clipboardData).getData('text');

    if(paste.length === 6){
        this.value = paste;
    }

});

</script>
</body>
</html>
