@model ResetPasswordVM
@{
    Layout = null;
    ViewData["Title"] = "Reset Password";
}
<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Password — Sillage</title>
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400&family=Jost:wght@200;300;400;500&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    @* <link href="~/css/users/customerpage.css" rel="stylesheet" />
    <link href="~/css/auth/auth.css" rel="stylesheet" /> *@

    <style>
        .reset-password-hero{
    min-height:60vh;
    display:flex;
    align-items:center;
    justify-content:center;
}

.reset-box{
    width:100%;
    max-width:420px;
    padding:0 24px;
    animation:fadeUp .5s ease;
}

.reset-step{
    text-align:center;
    font-size:12px;
    letter-spacing:.1em;
    text-transform:uppercase;
    color:var(--text3);
    margin-bottom:8px;
}

.reset-title{
    font-size:28px;
    text-align:center;
    margin-bottom:8px;
}

.reset-sub{
    text-align:center;
    margin-bottom:32px;
}

.password-wrap{
    position:relative;
}

.toggle-password{
    position:absolute;
    right:12px;
    top:50%;
    transform:translateY(-50%);
    cursor:pointer;
    opacity:.6;
    transition:.2s;
}

.toggle-password:hover{
    opacity:1;
}

.password-strength{
    width:100%;
    height:6px;
    background:#eee;
    border-radius:20px;
    margin-top:6px;
    overflow:hidden;
}

.password-strength span{
    height:100%;
    width:0%;
    display:block;
    background:red;
    transition:.3s;
}

.reset-btn{
    width:100%;
    justify-content:center;
    margin-bottom:16px;
}

.back-login{
    display:block;
    text-align:center;
}

@@keyframes fadeUp{
    from{
        opacity:0;
        transform:translateY(15px);
    }
    to{
        opacity:1;
        transform:translateY(0);
    }
}
    </style>
</head>
<body>


@*     <section class="hero" style="min-height:60vh;display:flex;align-items:center;justify-content:center;">
        <div style="width:100%;max-width:420px;padding:0 24px;">
            <p style="text-align:center;font-size:12px;letter-spacing:.1em;text-transform:uppercase;color:var(--text3);margin-bottom:8px;">Step 2 of 2</p>
            <h1 class="hero-title" style="font-size:28px;text-align:center;margin-bottom:8px;">Reset <em>Password</em></h1>
            <p class="hero-sub" style="text-align:center;margin-bottom:32px;">Enter your new password for <strong>@Model.Email</strong> below.</p>

            <div asp-validation-summary="ModelOnly" class="auth-msg auth-error"></div>

            <form asp-action="ResetPassword" asp-controller="Account" method="post" class="auth-form">
                @Html.AntiForgeryToken()

                <input type="hidden" asp-for="Email" />
                <input type="hidden" asp-for="Code" />

                <div class="auth-input-wrap">
                    <label asp-for="NewPassword" class="auth-label">New Password</label>
                    <input asp-for="NewPassword" type="password" class="auth-input" placeholder="••••••••" autocomplete="new-password" />
                    <span asp-validation-for="NewPassword" class="auth-validation"></span>
                </div>

                <div class="auth-input-wrap">
                    <label asp-for="ConfirmPassword" class="auth-label">Confirm Password</label>
                    <input asp-for="ConfirmPassword" type="password" class="auth-input" placeholder="••••••••" autocomplete="new-password" />
                    <span asp-validation-for="ConfirmPassword" class="auth-validation"></span>
                </div>

                <button type="submit" class="hero-cta" style="width:100%;justify-content:center;margin-bottom:16px;">Reset Password</button>
                <a asp-action="Login" asp-controller="Account" class="auth-link" style="display:block;text-align:center;">Back to Sign In</a>
            </form>
        </div>
    </section> *@

    <section class="hero reset-password-hero">

        <div class="reset-box">

            <p class="reset-step">Step 2 of 2</p>

            <h1 class="hero-title reset-title">
                Reset <em>Password</em>
            </h1>

            <p class="hero-sub reset-sub">
                Enter your new password for <strong>@Model.Email</strong> below.
            </p>

            <div asp-validation-summary="ModelOnly" class="auth-msg auth-error"></div>

            <form asp-action="ResetPassword" asp-controller="Account" method="post" class="auth-form">

                @Html.AntiForgeryToken()

                <input type="hidden" asp-for="Email" />
                <input type="hidden" asp-for="Code" />

                <!-- New Password -->

                <div class="auth-input-wrap">

                    <label asp-for="NewPassword" class="auth-label"></label>

                    <div class="password-wrap">
                        <input asp-for="NewPassword"
                               id="newPassword"
                               type="password"
                               class="auth-input"
                               placeholder="••••••••"
                               autocomplete="new-password" />

                        <span class="toggle-password" onclick="togglePassword('newPassword')">
                            👁
                        </span>
                    </div>

                    <div class="password-strength">
                        <span id="strengthBar"></span>
                    </div>

                    <span asp-validation-for="NewPassword" class="auth-validation"></span>

                </div>

                <!-- Confirm Password -->

                <div class="auth-input-wrap">

                    <label asp-for="ConfirmPassword" class="auth-label"></label>

                    <div class="password-wrap">
                        <input asp-for="ConfirmPassword"
                               id="confirmPassword"
                               type="password"
                               class="auth-input"
                               placeholder="••••••••"
                               autocomplete="new-password" />

                        <span class="toggle-password" onclick="togglePassword('confirmPassword')">
                            👁
                        </span>
                    </div>

                    <span asp-validation-for="ConfirmPassword" class="auth-validation"></span>

                </div>

                <button type="submit" class="hero-cta reset-btn">
                    Reset Password
                </button>

                <a asp-action="Login" asp-controller="Account" class="auth-link back-login">
                    Back to Sign In
                </a>

            </form>

        </div>

    </section>



    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/dist/jquery.validate.unobtrusive.min.js"></script>

    <script>

function togglePassword(id){

    const input = document.getElementById(id);

    input.type = input.type === "password" ? "text" : "password";

}


const password = document.getElementById("newPassword");
const bar = document.getElementById("strengthBar");

password.addEventListener("input",function(){

    let val = password.value;
    let strength = 0;

    if(val.length >= 6) strength++;
    if(/[A-Z]/.test(val)) strength++;
    if(/[0-9]/.test(val)) strength++;
    if(/[^A-Za-z0-9]/.test(val)) strength++;

    let width = strength * 25;

    bar.style.width = width + "%";

    if(width <= 25) bar.style.background = "red";
    else if(width <= 50) bar.style.background = "orange";
    else if(width <= 75) bar.style.background = "#4caf50";
    else bar.style.background = "#2e7d32";

});

</script>

</body>
</html>
